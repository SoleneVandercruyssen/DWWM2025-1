"use strict"

const painting = {
    btnsColor: [] ,

    createCanva (){
        // Créer le canva
        const canvas = document.createElement("canvas");
        const cva = canvas.getContext("2d");
    }

    
};